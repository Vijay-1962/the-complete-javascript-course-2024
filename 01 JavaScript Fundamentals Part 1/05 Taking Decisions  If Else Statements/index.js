const age = 15;
// const isOldEnough = age >= 18;

if (age >= 18) {
    console.log("Sarah can start driving licence 👍");
} else {
    const yearsLeft = 18 - age;
    console.log(`Sarah is too young for driving, wait another (${yearsLeft}) years 😃`);
}

const birthYear = 2001;
let century;
// if (birthYear <= 2000) {
if (birthYear <= 2000) {
    century = "20th";
} else {
    century = "21st";
}
console.log(century);

// if (BMIMark == BMIJohn) {

// } else if (M > J) {

// } else {
//     // M < J
// }
